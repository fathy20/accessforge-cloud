# ADR-001: Background Worker Pattern
**Context**: Desktop App2 froze the UI during PDF stamping.
**Decision**: Implement a jobs table polling mechanism. The frontend triggers a job, and an external worker processes the file and updates the row.
**Consequences**: Scalable, non-blocking UI, requires separate worker infrastructure.
