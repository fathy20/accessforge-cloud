# Decision Log
A chronological log of minor daily decisions.
- [2026-07-07]: Restructured MCP Memory to v3 (added entities, workers, UI isolation).
- [2026-07-07]: Decided to delay SQL Migrations until Excel analysis is complete based on user feedback.
- [2026-08-17]: Crew Hours owner rulings (Heavy). Q1: cabin trainee = Work Schedule Function == "SFA" — never Position-only (SFA the position is a normal senior cabin rank), never Position-SFA-AND-Function-TRN. Q2: EVN/SVX are FLIGHT-LEVEL absolutes — EVN forces No (vetoes even a cockpit-count Yes), SVX forces Yes, EVN wins if both; matched against ADEP/ADES airport codes, legacy tags kept as secondary signal. Q3: the {"SP","OPS"} cabin exclusion is removed (those are cockpit trainee slots). Consolidation: `heavy.classify_flight_heavy` is the single flight-level engine for BOTH the Crew Hours service and the Copilot; `cabin_heavy.py` is a one-release deprecation shim. STEP 4 (rotation resolver) runs ONLY on the count rule's UNKNOWN — it can never override a count-derived Yes. Known gap: LEON REJECTS the `workSchedule { function }` selection (live 2026-06 run), so cabin trainee detection does not fire in production; surfaced as `cabin_trainee_detection: "unavailable"` in report metadata. Follow-up open on LEON's side: enable the Function field. Full rationale: `accessforge-cloud-main/accessforge-cloud-main/docs/architecture/crew-hours-heavy-precedence-adr-2026-08-17.md`.
- [2026-08-18]: Crew Hours join key CONFIRMED live — `unique_id == flightNid == trNid`. id_probe run against live LEON days 2026-06-16/-20/-22: all three endpoints returned the same identifier (RSX331 = 67230742 in Report Wizard, flight list, and FTL); airports came back ICAO (HESH→USSS). A live June 2026 report then showed `join_health: OK`, `augmented_lookup_hits 1892/2493`, `crew_context_hits 2107/2493`. Settled — do not re-run the probe or re-decide the key; the hit-rate instrumentation stays only as a regression tripwire. ADR Decision 4 corrected to match (it previously said UNVERIFIED).
- [2026-08-18]: This ROOT `MCP_Memory/` tree is the ONE authoritative knowledge base. A nested single-file `MCP_Memory/` (only `development/decision-log.md`) had grown next to the app code at `accessforge-cloud-main/accessforge-cloud-main/` and was silently absorbing decisions the root KB never saw — that split is how settled findings got lost. Its entries were mirrored here and into `development/closed-questions.md`, then the nested tree was deleted (full text remains in git history, commits e30917e/06611b0; rationale lives in the Heavy-precedence ADR). Do not recreate a nested MCP_Memory.
- [2026-08-19]: Crew Hours Heavy — five owner rulings landed from the June 2026 UI review, each with a failing test first. (1) **D-1, airport aliases**: `SVX ↔ USSS`, `EVN ↔ UDYZ` in `positions.AIRPORT_CODE_ALIASES`; exact match on either form, never substring; `classify_flight_heavy` takes `route_airports` and unions the caller's codes with the context's, so the report row's preferred codes reach the rule on every surface. Correction to the diagnosis: the Copilot was NOT structurally blind — `_context_index_from_report` already copied the row's codes into its `FlightContext` — the defect was that neither surface could see a *second* form of one airport, so an ICAO-coded SVX leg read Not Heavy on both. Evidence: RSX331 HESH→USSS. (2) **D-2, crew continuity**: `_continuity_sets` compares each leg's operating crew UNION everyone present on BOTH legs in any capacity, plus the subject; the old position-filtered symmetric set equality deleted an FO→PAD member from one side and broke the rotation for EVERY member (RSX6077/RSX6078 HRG↔LIS 14-06). The ruling's literal "operating crew plus the subject" would not have produced the ruled outcome for the colleagues; the ruled outcome (YES on both legs for every member) was treated as authoritative. Riders on only one leg stay excluded; PSN keeps its immediate NO. (3) **True out-and-back**: `_rotation_out_and_back` requires departure AND arrival to mirror; a chain onward is not a rotation. Fixes a live wrong-Heavy class — RSX8891 HRG→SSH then RSX6083 SSH→OPO, break 0:50, identical roster → No on both, `ROTATION_MISMATCH`. Side effect: the old case-4 pair now reports `ROTATION_MISMATCH` instead of `BREAK_EXCEEDS_LIMIT`. (4) **Badge rule corrected**: `unknown_resolved = resolution.effective_heavy` — the badge means the resolver ESTABLISHED Heavy; a resolver No is absence of evidence and carries none. Retracts closed question 8's "every STEP-4 entry is badged". Export and tooltip re-keyed on the reason so their content is unchanged. (5) **Pairing direction**: backward always, forward only when nothing connected precedes the leg. (6) **`heavy_trace`** on every `FlightItem` — ordered steps with the inputs each rule saw, shown as a `Decision trace` disclosure on every leg's verdict cell; `heavy.py`/`unknown_resolver.py` stay pure via a dependency-free `trace.py`. Unchanged: thresholds `cockpit > 2`/`cabin > 4`, cabin trainee `Function == "SFA"`, EVN/SVX precedence, `break < 4h` strict, PSN immediate NO, block-time totals. Full rationale: Decision 6 in `accessforge-cloud-main/accessforge-cloud-main/docs/architecture/crew-hours-heavy-precedence-adr-2026-08-17.md`. Suites: 541 backend passed, 43 frontend passed.
- [2026-09-03]: Frontend weight + hygiene pass. The public landing page's WebGL globe (`src/components/FlightPathBackground/`, three.js + dat.gui + stats.js, a 5 MB route dataset and an 18 MB `earth.jpg`) was removed and replaced by a static CSS/SVG hero — it was the single heaviest asset in the app for a decorative effect. Deleted 25 never-imported shadcn primitives (sidebar, chart, form, calendar, carousel, command, drawer, …) and their Radix/vendor packages, `recharts` (dashboard now uses two small in-repo SVG charts under `src/components/charts/`, which also fixes the old `hsl(var(--x))` fills that resolved to invalid CSS), `playwright` (no e2e suite), `PlaceholderPage`/`ModulePlaceholder`, 12 dead i18n keys and `getBusinessAreaLabel`. Performance: the `/auth/me` guard now goes through `queryClient.ensureQueryData` (5-min cache) instead of one request per navigation; QueryClient gets `staleTime: 30s`, `refetchOnWindowFocus: false`; job lists poll 3 s only while a job is queued/running, else 20 s (was a flat 2–3 s on dashboard, jobs, every module page and task-extractor); `useAuth` no longer clears the token inside render; `usePermissions` is memoised. Structure: `PageHeader` is the one page-title block (adopted on 11 routes); jobs.tsx and ModuleJobs.tsx share `components/jobs/{JobList,job-status,use-job-status-toasts}`. Repo root: removed the 20 MB `accessforge-cloud-main.zip`, WhatsApp screenshots, duplicate logos (byte-identical to `public/`), early brainstorm notes `v_1*/v_2.txt`, `privileges_map.xlsx` (RBAC lives in `backend/rbac/registry.py`), an unused Supabase root `.env`, 19 `.codex-delegate-*` scratch dirs, `.runtime`, stale `bun.lock` (npm is the toolchain), `dist/`, agent brief files; the two July planning docs moved to `docs/architecture/legacy-*.md`. Kept on purpose: `Stactstics hany/` (export template + LEON wiki PDFs, cited by `export.py`), `refrance_output/` (validation sheets for the in-flight allowance export work), `app2.py` (legacy source the parity docs cite by line), `.lovable/`, `bunfig.toml`, `skills-lock.json`. Gates: tsc clean, vitest 55/55, `vite build` OK. Backend untouched (uncommitted crew-hours slice was in the tree).
- [2026-09-03]: Frontend is now a static SPA — no Node/nitro server. `vite.config.ts` sets `nitro: false`, `tanstackStart.spa = { enabled, maskPath: "/dashboard" }` and prerenders `/`, `/auth`, `/reset-password` as real HTML (plus one shell copy per static route path for deep links). `src/server.ts`, `src/start.ts`, `lib/error-capture.ts`, `lib/error-page.ts` and the `nitro` devDependency are gone. `ApiClient.API_URL` defaults to relative `/api`; the Vite dev server proxies it to `DEV_API_ORIGIN` (localhost:8000) and production must do the same behind one origin, which removes CORS from the deployment. Rationale: every authenticated route was already `ssr: false`, so the server only rendered three public pages. Runbook: `accessforge-cloud-main/accessforge-cloud-main/docs/deploy/frontend-static.md`. Next agreed steps in order: Dockerfile + nginx compose for the backend, `/api/dashboard/summary` aggregate endpoint + real pagination on `/api/jobs`, then the separate job worker (TECHNICAL_DEBT #1). Gates: tsc clean, vitest 55/55, `vite build` 26 pages prerendered, `dist/client` 1.6 MB.
- [2026-09-03]: Containerised deployment landed (perf plan step 2). `Dockerfile` = API image (python 3.11-slim, uvicorn with `WEB_CONCURRENCY` workers, Alembic at start via `deploy/api/entrypoint.sh`, Tesseract, optional msodbcsql18 via `INSTALL_MSSQL_DRIVER`); `Dockerfile.web` = static SPA on nginx 1.27 with `deploy/web/default.conf.template` proxying `/api` and `/health` to `API_UPSTREAM` (one origin → CORS out of the request path); `docker-compose.yml` wires them with a `data` volume. Gotchas recorded: (a) the Windows-generated `package-lock.json` only lists win32 native bindings (npm/cli#4828), so the web build installs the locked-version `linux-<arch>-gnu` bindings for rolldown / @tailwindcss/oxide / lightningcss after `npm ci` — regenerating the lock on Linux/CI removes that step; (b) `db_bootstrap` refuses a SQLite file named `redsea.db`, so compose defaults to `/data/app.db` and uses `COMPOSE_DATABASE_URL` (not `DATABASE_URL`) for overrides because the developer `.env` points at a host-relative file; (c) with 2 workers `startup_db_seed` logs one tolerated UNIQUE-conflict traceback on the registry projection — expected, documented in its docstring; (d) passlib 1.7.4 + bcrypt 4.1.2 prints the known `__about__` AttributeError trace once at first hash; harmless. Verified end-to-end on Docker Desktop: images api 781 MB / web 76 MB, `/health/ready` → `migration: current`, prerendered `/` served by nginx, deep link `/dashboard` → shell, bootstrap admin + login + dashboard render through the proxy. Runbook `docs/deploy/docker.md`; ARCHITECTURE.md got a Deployment section.
- [2026-09-03]: Dashboard aggregation moved server-side (perf plan step 3). New `GET /api/dashboard/summary` groups in SQL: `jobs.{total, active, by_status, by_module (top 8), daily (14 UTC days of done/failed, bucketed in Python from two columns to stay dialect-neutral), recent (6)}`, `uploads.{total, recent (6)}`, `projects.total` (shared count, mirrors `/api/projects` visibility). `GET /api/jobs` gained `offset` and an `X-Total-Count` header (body stays a list; header exposed through CORS). Frontend dashboard now makes one request instead of downloading every job and upload row per poll. Tests: `backend/tests/test_dashboard_summary.py` (scoping, window, ordering, pagination). Full backend suite 680 passed (5:41); frontend tsc clean, vitest 55/55. Remaining agreed step: the separate job worker (TECHNICAL_DEBT #1).
- [2026-09-03]: Durable job execution landed (perf plan step 4, TECHNICAL_DEBT #1). Migration `d1e2f3a4b5c6` adds `jobs.attempt / lease_owner / lease_token / lease_expires_at / heartbeat_at / cancel_requested` + index `ix_jobs_status_created_at`; offline SQL Server upgrade+downgrade DDL rehearsed (BIT/DATETIMEOFFSET, default-constraint drop via `mssql_drop_default`). `backend/job_queue.py`: claim = conditional single-row UPDATE (`queued` OR `running AND lease_expires_at <= now`) that stamps a per-claim `lease_token` and increments `attempt`; every later write is `fenced_update(... WHERE lease_token = :token)`; `finish` clears the token; `JOB_MAX_ATTEMPTS` (3) turns a repeatedly-reclaimed job into `failed`. `backend/job_runner.py` holds `execute_job(job_id, lease_token=None)` (moved out of main.py; `run_job_background`, `MAX_JOB_LOG_ENTRIES`, `_append_job_log` re-exported; module visibility helpers moved to `backend/module_visibility.py`). `worker/runner.py` (`python -m worker.runner`) spawns `worker/job_child.py` per job and supervises it: heartbeat extends the lease (lost lease → kill child, write nothing), `cancel_requested` → process-tree kill (`taskkill /T` on Windows, `killpg` on POSIX) → `cancelled`, `JOB_TIMEOUT_SECONDS` → kill → `failed`, SIGTERM → kill + `release_for_requeue`; a child that exits without a terminal write is recorded as `failed` via the same fence (no-op if it finished). API: `JOB_EXECUTION_MODE=inline|worker` (default inline, so tests and single-dev setups are unchanged); new `POST /api/jobs/{id}/cancel` (queued → cancelled now; running → flag) and `POST /api/jobs/{id}/retry` (failed/cancelled → queued, attempts keep counting); `_job_payload` gained `attempt` + `cancel_requested`; the frontend job list wires both buttons. Compose: `worker` service = api image with `entrypoint python -m worker.runner`, api gets `JOB_EXECUTION_MODE=worker`, `stop_grace_period 60s`. Verified in Docker end to end: enqueue → worker claim → handler error surfaced, cancel queued → cancelled, retry → attempt 2. Tests: `backend/tests/test_job_queue.py` (13, with real child processes); `test_check_control` runner patches repointed to `backend.job_runner`. Semantics stated as at-least-once. OPEN: claim/locking only exercised on SQLite — run `test_job_queue` and a two-worker soak on real SQL Server before production; orphan output files on crash between persist and publish (closes with debt #2). Also fixed the same day: `.dockerignore` lacked `**/.env`, so `backend/.env` had been copied into the API image — now excluded and verified absent.
- [2026-09-03]: Heavy re-derived from the regulation (PLAN, no verdict change yet). The owner's `reference.pdf` is EgyptAir OM Chapter 7 (STD SEP.09) = ECAR 121 Subpart Q = CAP 371: FDP runs 1:30 before first STD to 0:30 after last landing; max FDP by Table A (acclimatised, local start band × sectors) or Table B (rest band × sectors); extension by in-flight relief (≥3 h rest: +½ bunk cap 18 h / +⅓ seat cap 15 h; cabin 19/16) or split duty (3–10 h ground rest → +½); positioning = duty not sector, and counts inside the FDP when it precedes it; min rest = max(preceding duty, 12 h), 11 h away; cabin FDP +1 h; augmented cabin = minimum +50%. **A rotation is Heavy iff planned FDP > Table max; the operator then augments (LEON crewAugmentation) or carries two crews (the swap the allowance model credits).** Back-test on the ten owner cases with times (SVX, CGN, LIS, VKO, ALA, LED, EVN, KRR, OPO ×2): 10/10 agree with the rulings using only the inequality — no airport special cases, no 4 h sector minimum, no 3 h link. Divergences needing rulings: same-duty width (4 h vs "less than minimum rest" + split-duty extension), start-band dependence of SVX, OM-2009 vs ECAR-2016 table cells, acclimatisation history for Table B, positioning-before-FDP, cabin threshold (>4 vs minimum×1.5 = 6), Egypt DST. Plan and seven rulings (R1–R7) in `accessforge-cloud-main/accessforge-cloud-main/docs/architecture/heavy-fdp-regulatory-model-plan-2026-09-03.md`: pure `fdp.py` with versioned tables → shadow fields + trace → June/July back-test → rulings → promotion. OM pages 7.1-5..7.1-9 are missing from the scan.
- [2026-09-03]: FDP model implemented in SHADOW. `backend/statistics/crew_hours/fdp.py` (pure): Tables A/B as versioned data (`om-2009` from the owner's scan is the default; `ecar-2016` selectable with `CREW_HOURS_FDP_TABLES`), FDP window 1:30 before first STD → 0:30 after last STA, local start band with Egypt DST (UTC+3 last-Friday-April → last-Thursday-October since 2023), sector clamp, Table B by preceding rest (OM: <18 h has no row; ECAR: first row covers it), cabin +1:00, in-flight relief (½ bunk cap 18/19, ⅓ seat cap 15/16) and split-duty (3–10 h → ½) helpers, `rotation_window` (every leg a sector) vs `member_window` (rides before count in the FDP, not as sectors; rides after are outside). Service paints `FlightItem.fdp_shadow` {planned, limit, margin, band, sectors, table, needs_augmentation, agrees_with_verdict} on every leg of each allowance duty and appends `FDP_SHADOW_WINDOW/LIMIT/VERDICT` trace steps — after the existing VERDICT step, never before it. 29 tests (`test_crew_hours_fdp.py`): the ten owner cases, band edges, DST edges, member vs rotation windows, both table versions, Table B, cabin, extensions, trace shape, and the service painting; all 283 existing crew-hours tests unchanged. No verdict, export cell, or credit changed. Next: back-test June/July live and bring the disagreement list to the owner for R1–R7.
- [2026-09-03]: FDP shadow back-test run LIVE on June+July 2026 (`python -m backend.statistics.crew_hours.tools.fdp_backtest`, new tool; outputs kept OUT of the repo in `E:\work\REDSEA\web\Heavy_FDP_Report\` + `refrance_output/fdp_backtest/`). 5,390 member-legs: agree 3,226; shadow-No/verdict-Heavy 1,219; shadow-Heavy/verdict-No 937; no times 8. Credit vs FDP: agree 3,995; credited-but-FDP-not-heavy 1,267; FDP-heavy-not-credited 120. The disagreements cluster in three explainable buckets, none a calculation error: (1) **981 cabin legs (FA1–FA4) on single-sector VKO/LED flights** — cockpit flies out-and-back with 3 pilots (on 239 of those flights the cockpit has 2 sectors), cabin swaps one leg each; LEON's flight-level `crewAugmentation` paints the cabin leg Heavy and credits it (rule a) while the member's own duty is 1 sector ≈ 7:50 vs 13:00. The July manual sheet validated COCKPIT only; cabin H.C on these legs was never validated → new owner ruling R8. (2) **Rider legs (PAD/PSN) inside heavy duties** (690 with reason UNKNOWN): leg verdict answers "operated augmented?" = No, rotation FDP answers "needs Heavy?" = Yes; the allowance already credits them, so no money differs. (3) **120 genuine edge cases for the compliance view**: night VKO→HRG→CAI 10:55 vs 10:15 with LEON not augmented; EVN night pair 02/03-06 at 10:45 vs 10:15 (the "EVN never" policy is 30 min over the book at that timing); ENG1/ENG2 on QFF charter days of 3–4 sectors. Arabic explainer for the owner: `E:\work\REDSEA\web\Heavy_FDP_Report\Heavy_FDP_Explained.docx` (generated from the results; outside the repo by request).
- [2026-09-03]: FDP model VERIFIED against LEON's own FTL engine (June 2026, 1,562 flown member-duties via `ftl.dutyList` fields fdpStartTime/fdpEndTime/fdpLength/maxFdpLength/fdpExtension/splitDutyTime/discretionLength/sectorCount/isAcclimated/localTimeOffset/restFacility/crewAugmentation/isCabinCrew + sector reportingTime; tool `tools/fdp_leon_compare.py`, results outside the repo). Cockpit Table A limit matched LEON's maxFdp in 524/547 (96%); every remaining mismatch is split duty (+½ ground rest), commander's discretion, the augmentation cap (SEAT: 15:00 cockpit / 16:00 cabin), or a band taken in another acclimatisation zone. **LEON is configured with the OM-2009 values (12:30 for 3 sectors 08:00–14:59), which settles R1.** Cabin +1:00 confirmed. Two of my assumptions were wrong for Red Sea's configuration: FDP start is the per-flight/station reporting time (1:05–2:55, mode 1:30), not a constant, and FDP ends at on-blocks (post-flight 0:00, not 0:30) — with those, the EVN night pair 02/03-06 is exactly 10:15 = the limit, i.e. NOT Heavy, agreeing with the owner's rule. Everyone was acclimatised in June (Table B unused). `crewAugmentation`: NORMAL 783 / AUGMENTED 39 / DOUBLED 740 — cabin DOUBLED on Moscow/St Petersburg single-sector duties is genuine double complement, not an artefact; cockpit ENGM rotations are augmented at 12:45 < 13:15 (augmentation is a planning decision with margin). DESIGN CONSEQUENCE (recommended, new ruling R9): LEON FTL fields become the primary regulatory-Heavy source; the local fdp.py model becomes a validator/fallback fed with LEON's report times and acclimatisation offset; the allowance stays a separate explicit policy layer (who/unit/exceptions). Explainer doc updated: `E:\work\REDSEA\web\Heavy_FDP_Report\Heavy_FDP_Explained.docx` section 13.
